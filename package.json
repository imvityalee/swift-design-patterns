{
  "name": "swift-design-patterns-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing a catalog of GoF design patterns with idiomatic Swift examples and recommendation tools.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node18 --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" --outfile=dist/index.js",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
